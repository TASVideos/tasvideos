@model PlayerPointsTable
<div class="table-container">
	<table class="table table-striped table-bordered">
		<tr>
			<th>@Html.DisplayNameFor(m => m.Authors.First().Position)</th>
			<th>@Html.DisplayNameFor(m => m.Authors.First().Player)</th>
			<th>@Html.DisplayNameFor(m => m.Authors.First().Points)</th>
			<th>@Html.DisplayNameFor(m => m.Authors.First().Rank)</th>
		</tr>
		@foreach (var item in Model.Authors)
		{
			<tr>
				<td>@item.Position</td>
				<td>@item.Player</td>
				<td>@item.Points</td>
				<td>@item.Rank</td>
			</tr>
		}
	</table>
</div>

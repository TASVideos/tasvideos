@page
@section PageTitle {
}

<cache enabled="@(!User.Identity.IsAuthenticated)" expires-after="@TimeSpan.FromSeconds(Durations.FiveMinutesInSeconds)">
	@await Component.RenderWiki("System/FrontPage")
</cache>
@page "{id}"
@model MoveModel
@{
	ViewData["Title"] = $"Moving Topic: {Model.Topic.TopicTitle}";
}
<partial Name="_ForumHeader" />
<h4>
	Moving Topic: 
	<a asp-page="/Forum/SubForum/Index" asp-route-id="@Model.Topic.ForumId">@Model.Topic.ForumName</a> -> 
	<a asp-page="Topic" asp-route-id="@Model.Id">@Model.Topic.TopicTitle</a>
</h4>
<hr />
<form form-method="post">
	<input type="hidden" asp-for="Topic.TopicTitle" />
	<input type="hidden" asp-for="Topic.ForumName" />
	<form-group class="col-lg-6">
		<label asp-for="Topic.ForumId" class="form-control-label"></label>
		<select asp-for="Topic.ForumId" asp-items="Model.Topic.AvailableForums.OrderBy(f => f.Text)" class="form-control"></select>
		<span asp-validation-for="Topic.ForumId" class="text-danger"></span>
	</form-group>
	<div class="text-center">
		<button type="submit" class="btn btn-primary">Move</button>
		<a asp-page="Topic" asp-route-id="@Model.Id" class="btn btn-secondary">Cancel</a>
	</div>
</form>

@section Scripts {
	<partial name="_ValidationScriptsPartial" />
}

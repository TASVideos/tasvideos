@page "{role?}"
@model IndexModel
@{
	ViewData.SetTitle("Role - " + Model.RoleViewModel.Name);
}

<a class="btn btn-secondary mb-2" asp-page="List">All Roles</a>
<partial name="_Role" , model="Model.RoleViewModel" />

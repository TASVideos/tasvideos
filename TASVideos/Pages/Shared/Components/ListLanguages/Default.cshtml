@model IEnumerable<TASVideos.RazorPages.ViewComponents.Models.LanguageEntry>

@foreach (var l in Model)
{
	<text>
		>> <a href="/@l.Path">@l.LanguageCode | @l.LanguageDisplayName</a>
		<br />
	</text>
}
@section PageTitle {
}

<cache enabled="@(!User.Identity.IsAuthenticated)" expires-after="@TimeSpan.FromSeconds(DurationConstants.OneMinuteInSeconds)">
	@await Component.InvokeAsync(typeof(RenderWikiPage), new { url = "System/FrontPage" })
</cache>